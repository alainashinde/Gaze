{
  "manifest_version": 3,
  "name": "Gaze Focus Notifier (Dev)",
  "version": "1.0",
  "description": "Polls local Gaze server and notifies when user loses focus or leaves focus tab.",
  "permissions": ["tabs", "notifications", "storage"],
  "host_permissions": ["http://127.0.0.1:5000/*"],
  "background": { "service_worker": "background.js" },
  "action": { "default_popup": "options.html" }
}
